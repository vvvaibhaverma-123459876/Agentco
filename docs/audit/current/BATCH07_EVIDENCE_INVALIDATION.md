# Batch 07 Evidence Invalidation

Campaign `cross-version-civilization-v1` is invalidated.

Invalidated artifact IDs:

- `8314475888`
- `8314717239`

Invalidated decision: `PROMOTION_PROPOSAL`

Reason: subject results were generated by evaluator-side deterministic rules
rather than subject-native execution.

Invalid logic identified:

- `deterministic_output()` generated candidate labels and confidence.
- Subject-specific conditions for `version-a`.
- Subject-specific conditions for `version-b`.
- Feature-derived latency.
- Feature-derived resource use.
- Constant authorization, budget, tool reliability and recovery scores.

The evidence remains preserved for audit history but is superseded by
`real-cross-version-civilization-v1`.
