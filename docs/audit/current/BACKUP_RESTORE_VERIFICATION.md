# Backup Restore Verification

Runtime evidence is generated by `make audit-staging-deployment` under `artifacts/staging-deployment/<run-id>/backup-restore/`. The local audit creates application state, captures `pg_dump`, restores into an isolated restore database, and validates known records and outbox state.
