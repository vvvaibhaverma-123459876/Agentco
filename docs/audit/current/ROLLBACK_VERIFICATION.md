# Rollback Verification

Runtime evidence is generated by `make audit-staging-deployment` under `artifacts/staging-deployment/<run-id>/rollback/`. The local audit deploys image A, rolls to image B, introduces a bad readiness release, performs `kubectl rollout undo`, and verifies retained state.
