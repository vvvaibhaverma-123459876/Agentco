# Hosted Identity Access Matrix

Status: `BLOCKED`.

Required identities are deployment automation, migration job, backend runtime,
outbox worker, frontend, monitoring, backup job, read-only operator, and
incident operator. Denied-operation evidence must come from the hosted provider
and Kubernetes API, not from static policy text.
