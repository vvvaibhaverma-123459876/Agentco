#!/usr/bin/env python3
"""
Learner CLI - Runs the learner on a replay batch to generate candidates
"""

import sys
import os

print("⚠️  Learner CLI is scaffolded but learner logic is implemented via the orchestrator.")
print("   For production learner runs, use:")
print("   - make autonomy-level3-smoke  (includes learner in full loop)")
print("   - Python services: backend/src/services/learner.service.ts")
print()
print("   This script would call the learner service with a specific replay batch ID.")
print("   Example: python3 run_learner.py --batch-id <batch-id>")

sys.exit(0)
