# External Data Boundary

This directory is for external or frozen datasets used by research, demos, or acceptance traces.

Datasets here are not part of the production runtime by default. Core product behavior should not depend on these files unless the dependency is documented by the consuming script or test.

Large datasets may be moved to a separate research archive later.
